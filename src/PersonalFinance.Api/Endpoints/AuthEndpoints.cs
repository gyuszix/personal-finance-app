namespace PersonalFinance.Api.Endpoints;

// POST /api/v1/auth/register | login | refresh | logout
// Issues 15-min JWTs + rotating refresh tokens (hashed in DB, reuse detection).
public static class AuthEndpoints
{
}
