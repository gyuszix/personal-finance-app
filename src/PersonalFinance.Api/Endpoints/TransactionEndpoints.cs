namespace PersonalFinance.Api.Endpoints;

// GET   /api/v1/transactions?after=<id>&limit=50  — cursor-paginated, newest first
// PATCH /api/v1/transactions/{id}/category        — set user_category_override
public static class TransactionEndpoints
{
}
