<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="PersonalFinance.App.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:PersonalFinance.App.Views"
    FlyoutBehavior="Disabled"
    Title="PersonalFinance.App">

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate views:LoginPage}"
        Route="login" />

    <ShellContent
        Title="Register"
        ContentTemplate="{DataTemplate views:RegisterPage}"
        Route="register" />

    <TabBar>
        <ShellContent
            Title="Dashboard"
            ContentTemplate="{DataTemplate views:DashboardPage}"
            Route="dashboard" />

        <ShellContent
            Title="Transactions"
            ContentTemplate="{DataTemplate views:TransactionsPage}"
            Route="transactions" />

        <ShellContent
            Title="Accounts"
            ContentTemplate="{DataTemplate views:AccountsPage}"
            Route="accounts" />
    </TabBar>

</Shell>